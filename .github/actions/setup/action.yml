name: 'Setup Node project'
description: 'Setup Node.js avec cache npm et installation des dépendances'
runs:
  using: 'composite'
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: 'lts/*'
        cache: 'npm'
    - run: npm ci --prefer-offline
      shell: bash
